<?
    $default_config = function(): array {
        return [
            'size'        => 'A4',
            'is_portrait' => true,
            'always_on'   => false // If true, no button will be shown but print mode is always on
        ];
    }
?>