<?
    $default_config = function(): array {
        return [
            'res_module' => null,  // The module that holds the favicon files under `res/`
                                   // If this is null, the currently set template is used
        ];
    }
?>