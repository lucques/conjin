# nginx proxy

Run this development Compose project through `./bin/compose`, for example `./bin/compose up --detach` and `./bin/compose down`. The wrapper imports the centrally managed nginx-proxy image from `composer-projects/conjin/deployment/dhall/docker-images.dhall`.
