services:
  nginx-proxy:
    image: "${IMAGE_NGINX_PROXY:?Run this Compose project through ./bin/compose}"
    container_name: nginx-proxy
    restart: always
    ports:
      - "127.0.0.1:80:80"
      - "127.0.0.1:443:443"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
