# references

This module allows to easily tag content with their references. The references can then be listed e.g. at the end of the page.


## How to support this module
If a template supports this module, it should:
- Render the references at the end of the page using `ref_print_list`