# subpages-all

This module reads the directory of the current target and adds every subdirectory except `inc` and `res` as a subpage, ordered by directory name. This avoids listing each subpage manually.

See the [demo page](../../../../demo-app/src/content/module-demos/subpages-all/index.php).
